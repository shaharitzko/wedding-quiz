<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    
</head>
<body>
    <% if (locals.question) { %>
        <form action="/" method="post">
          <%= question.question %>
          <br>
          <% const answers = question.answers; %>
          <div class="btn-group-vertical" role="group" aria-label="Vertical radio toggle button group">
            <% for (let j = 0; j < answers.length; j++) { %>
              <% const radioName = "vbtn-radio" + (j+1)%>
              <input type="radio" class="btn-check" name="vbtn-radio" id=<%= radioName %> autocomplete="off" value=<%= j %>>
              <label class="btn btn-outline-dark" for=<%= radioName %>><%= answers[j].answer %></label>
            <% } %>
          </div>
          <br>
          <button class="btn btn-primary" type="submit">שאלה הבאה</button>
        </form>
    <% } else { %>
      <h1>Error</h1>
    <% } %>

    <!-- <div class="btn-group-vertical" role="group" aria-label="Vertical radio toggle button group">
      <% for (let i = 1; i <= 3; i ++) { %>
        <% const radioName = "vbtn-radio" + i%>
        <input type="radio" class="btn-check" name="vbtn-radio" id=<%= radioName %> autocomplete="off">
        <label class="btn btn-outline-danger" for=<%= radioName %>>Radio <%= i %></label>
      <% } %>
    </div> -->
    
</body>
</html>